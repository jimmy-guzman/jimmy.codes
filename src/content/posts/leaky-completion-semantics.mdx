---
title: Leaky Completion Semantics
publishDate: 2026-03-16
description: "When a function's return or throw behavior encodes hidden policy decisions, callers inherit rules they never opted into."
keywords:
  [
    "leaky abstractions",
    "function design",
    "API design",
    "TypeScript",
    "best practices",
  ]
tags: ["TypeScript"]
---

Most bugs I see in mature codebases are not caused by bad logic.

They are caused by APIs that lie. Not maliciously. Quietly. Over time.

This post is about a specific kind of lie: leaky completion semantics.

> [!TLDR]
> Leaky completion semantics happen when a function's return or throw behavior encodes hidden policy decisions. Callers must understand those decisions to use the function safely.

## A small function with a hidden rule

```ts
async function getFruit(fruit: string) {
  if (fruit === "apple") {
    return null;
  }

  return fetch(`https://api.fruitinfo.com/${fruit}`);
}
```

Nothing about this function's name or signature tells you that apples return `null`.

You only learn that by reading the implementation, hitting it at runtime, or being told by someone who already knows.

> [!NOTE]
> `null` here is not a value. It is a sentinel encoding a routing decision.

Six months later, someone writes:

```ts
async function getFruitData(fruit: string) {
  const result = await getFruit(fruit);

  return { data: result };
}
```

Now `getFruitData` also returns `null` for apples. Every caller of `getFruitData` has inherited a rule they never opted into.

### "Just throw instead"

A common fix is to throw instead of returning `null`:

```ts
async function getFruit(fruit: string) {
  if (fruit === "apple") {
    throw new Error("Apples not supported");
  }

  return fetch(`https://api.fruitinfo.com/${fruit}`);
}
```

This seems better. TypeScript is happier. The null checks disappear.

But now every caller must know:

```ts
try {
  const fruit = await getFruit(name);
  // handle fruit
} catch (error) {
  // is this a routing error or a real error?
  // should I retry? route elsewhere? fail?
}
```

The problem is the same. The function still decides whether work should happen, still encodes "apples are special" in its completion behavior, still forces callers to understand hidden routing rules.

Throwing instead of returning `null` just moves the leak from the return channel to the error channel. The problem is not what the function returns. It is what the return means.

## What leaked?

A function has leaky completion semantics when the rules that govern how it completes encode hidden routing or policy decisions. Returns, throws, short-circuits: if callers must understand those rules to use the function correctly, something leaked.

When those rules are not obvious from the function's name or signature, the function's internal logic becomes part of its external API. That is the leak.

Joel Spolsky described [leaky abstractions](https://www.joelonsoftware.com/2002/11/11/the-law-of-leaky-abstractions/) in terms of performance and system mechanics bleeding through. This is a narrower version of the same idea. Here, the abstraction leaks policy decisions through how it completes. It fails not because it cannot hide complexity, but because it hides meaning.

### Completion semantics

Completion semantics are the rules that determine how and when a function completes: what it returns, what it throws, which inputs short-circuit execution, and which special cases exist.

In the example above, the rule is: apples are special and do not behave like other fruits. That rule lives inside `getFruit`, but it affects every caller downstream.

## Single responsibility, but sharper

This is often framed as a Single Responsibility Principle violation, but that framing is too soft.

What is really happening: the function is both deciding whether work should happen and doing the work.

`getFruit` claims to fetch fruit data. It also secretly decides which inputs are allowed to proceed. Those are two different semantic roles. A function should not quietly do both unless routing is the thing it claims to do.

## Put the decision where it belongs

```ts
async function getFruitData(fruit: string) {
  if (fruit === "apple") {
    return null;
  }

  return getFruit(fruit);
}

async function getFruit(fruit: string) {
  return fetch(`https://api.fruitinfo.com/${fruit}`);
}
```

Now the semantics are honest:

- `getFruit` fetches
- `getFruitData` decides when fetching makes sense

Callers of `getFruitData` do not need to know anything about apples. The function owns the decision.

This does not mean "hoist every conditional to the caller." The goal is not to remove policy from functions. It is to make sure the function that owns a policy is the one named for it. If apple-routing is a real domain concern, give it a home: a guard function, a middleware layer, a named check. The problem is not that policy exists. It is that policy hides inside a function whose name promises something else.

## When this is not a problem

Not all conditional behavior is leaky.

```ts
function parsePositiveNumber(value: unknown): number {
  const num = Number(value);

  if (!Number.isFinite(num) || num <= 0) {
    throw new Error("must be positive number");
  }

  return num;
}
```

This is fine. The name encodes the constraint. Failure is intrinsic to parsing. The completion semantics are obvious without reading the implementation.

This is validation, not routing. Leaky completion semantics are about policy decisions, not domain truths.

## The real test

Three questions to ask:

Do callers need to understand internal rules to handle the result correctly? If yes, something leaked.

Does the function do work, or decide whether work should happen? If it quietly does both, the boundary is wrong.

Would this behavior surprise someone who only read the function name? I still get this wrong sometimes. The tell is when I find myself checking conditions before calling a function, or when a code review reveals a special return value I did not know about. The surprise is the design smell.

## Everything is an API

Internal functions are APIs. They just have worse documentation, fewer reviews, and more tribal knowledge.

You would not ship a public API that returns special values for undocumented reasons. You would not tell customers "just know not to send apples." Do not do that to your future self or your teammates.

## Keep routing explicit

Functions that fetch should fetch. Functions that route should route and be named accordingly. When you mix these, the completion logic leaks. And leaks spread.

If you find yourself checking conditions before calling a function, question the boundary. If a function returns special values to signal a "wrong path," question the routing. If callers need internal rules to use it safely, the abstraction leaked.

Everything is an API. Even code you are "just supposed to know."
